services:
  postgres:
    image: postgres:17

    container_name: encurtaai-postgres

    environment:
      POSTGRES_DB: encurtaai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: